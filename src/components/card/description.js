export default function Description({ description }) {
    return (
        <div>
            <p className="text-sm mx-2 mt-2 mb-2 text-slate-400 line-clamp-2 ">
                {description}
            </p>
    </div>

    );
}
